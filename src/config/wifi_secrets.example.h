#pragma once

// Copy this file to wifi_secrets.h and fill local credentials.
// wifi_secrets.h is ignored by git.
constexpr const char* WIFI_SSID = "YOUR_WIFI_SSID";
constexpr const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";
